<aside>
	<div id="sidebar" class="nav-collapse ">
		<ul class="sidebar-menu">
			<% menus.forEach((menu) => { %>
				<li>
					<a href="<%= menu.url %>">
						<i class="<%= menu.icon %>"></i>
						<span><%= menu.name %></span>
					</a>
				</li>
			<% }) %>
		</ul>
	</div>
</aside>
